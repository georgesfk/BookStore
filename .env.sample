# Copy this file to `.env` and edit values for your environment.

# --- Postgres (used by docker-compose) ---
POSTGRES_DB=bookstoredb
POSTGRES_USER=bookstore_user
POSTGRES_PASSWORD=bookstore_password

# --- Backend ---
# If not set, the backend entrypoint will generate a random JWT_SECRET for local development.
JWT_SECRET=change_me_in_production
# Spring datasource (when using docker-compose, these point to the Postgres service)
SPRING_DATASOURCE_URL=jdbc:postgresql://bookstore-db:5432/bookstoredb
SPRING_DATASOURCE_USERNAME=bookstore_user
SPRING_DATASOURCE_PASSWORD=bookstore_password

# --- Frontend ---
# When running the frontend container, this should point to the backend service URL inside docker-compose
API_URL=http://bookstore-backend:8080/api/v1

# Optional: override host ports used by docker-compose
#HOST_FRONTEND_PORT=4200
#HOST_BACKEND_PORT=8080

# Notes:
# - For reproducible local runs, set a stable JWT_SECRET in this file.
# - Do NOT use these example credentials in production. Use a secrets manager or CI/CD environment variables.
